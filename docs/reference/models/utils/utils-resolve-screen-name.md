<div class="vk-card">

# Объект UtilsResolveScreenName

Представляет результат резолва короткого имени в OpenVK.

</div>

<div class="vk-card">

# Поля объекта

| Поле / Тип | Описание |
| :--- | :--- |
| **ObjectId** <br> `int` | Идентификатор найденного объекта. |
| **Type** <br> `string` | Тип найденного объекта (например, `"user"` — пользователь, `"group"` — сообщество, `"application"` — приложение). |

</div>
